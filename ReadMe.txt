Dependencies:
    .NET Core 3.1
    RabbitMQ running in localhost

To execute the project:
    Execute the command 'dotnet run' into root folder (ChatRoom); OR
    Open in Visual Studio 2019, by ChatRoom.sln, and press F5;
